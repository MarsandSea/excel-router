# docs 资源目录

放置 README 里引用的截图和演示动图。

## 待补充（请在本机录制）

- [ ] `screenshot.png` —— 程序主界面截图（运行 `python main.py` 后截一张）。
- [ ] `demo.gif`（可选）—— 演示「选输入文件夹 → 重新识别列 → 选主拆分列 → 开始处理」的动图。

> 提示：有截图/动图的开源项目 Star 转化明显更高。
> 录屏可用 Windows 自带 Xbox Game Bar（Win+G），转 GIF 可用 ScreenToGif 等免费工具。
> 截图建议用 `examples/` 里的样本数据演示，避免泄露真实业务信息。
